<%= form_for(topic) do |f| %>
  <% if topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :codename %><br />
    <%= f.text_field :codename, class: "form-control"%>
  </div>
  <div class="form-group">
    <%= f.label :type %><br />
    <%= f.number_field :type, class: "form-control" %>
  </div>
  <br />
  <div class="actions">
    <%= f.submit I18n.t('admin_panel.create_user') %>
  </div>
<% end %>
